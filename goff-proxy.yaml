# GO Feature Flag Relay Proxy
pollingInterval: 1000

retrievers:
  # Flutter flags
  - kind: file
    path: /goff/flags/apps/flutter.yaml

  # API flags
  - kind: file
    path: /goff/flags/apps/api.yaml

  # Shared flags
  - kind: file
    path: /goff/flags/shared.yaml

