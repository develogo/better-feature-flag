# GO Feature Flag Relay Proxy
pollingInterval: 1000

retrievers:
  # Flutter flags
  - kind: file
    path: /goff/flags/flutter.yaml

  # Shared flags
  - kind: file
    path: /goff/flags/shared.yaml

