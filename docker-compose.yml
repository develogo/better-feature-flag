services:
  go-feature-flag:
    image: gofeatureflag/go-feature-flag:latest
    ports:
      - "1031:1031"
    volumes:
      - ./flags:/goff/flags
      - ./goff-proxy-dev.yaml:/goff/goff-proxy.yaml
    restart: unless-stopped