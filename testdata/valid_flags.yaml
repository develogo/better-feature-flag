apps:
  flutter:
    flags:
      - name: dark_mode
        type: bool
        default: false
      - name: app_version
        type: string
        default: "1.0.0"
  backend:
    flags:
      - name: cache_enabled
        type: bool
        default: true
